# Backend API

See the [project README](../README.md) for setup and architecture. Run `python seed.py` to reset the demo database, then `python app.py` to serve the API on port 5000.

Endpoints: `GET /health`, `GET /schema`, `POST /generate-query`, and `POST /execute-query`.
