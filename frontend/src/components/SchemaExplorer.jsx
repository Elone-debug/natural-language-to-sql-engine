import {Database,ChevronRight} from 'lucide-react';
export default function SchemaExplorer({tables}){return <section className="card p-4"><h2 className="mb-3 flex items-center gap-2 font-semibold"><Database size={17}/> Database schema</h2><div className="space-y-2">{tables.map(t=><details key={t.name} open className="group rounded-xl border border-slate-100"><summary className="flex cursor-pointer list-none items-center justify-between px-3 py-2 text-sm font-semibold">{t.name}<ChevronRight size={14} className="transition group-open:rotate-90"/></summary><ul className="border-t border-slate-100 px-3 py-2">{t.columns.map(c=><li key={c.name} className="flex justify-between py-1 text-xs"><span>{c.name}</span><span className="text-slate-400">{c.type}</span></li>)}</ul></details>)}</div></section>}
