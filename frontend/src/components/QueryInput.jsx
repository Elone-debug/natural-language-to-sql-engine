import {Sparkles} from 'lucide-react'; import LoadingSpinner from './LoadingSpinner';
export default function QueryInput({value,onChange,onGenerate,loading}){return <section className="card p-5"><label className="mb-2 block text-sm font-semibold">Ask your database</label><textarea maxLength={500} value={value} onChange={e=>onChange(e.target.value)} placeholder="e.g. Find the top 5 customers by total spend" className="min-h-30 w-full resize-y rounded-xl border border-slate-200 bg-slate-50 p-4 outline-none transition focus:border-forest focus:ring-3 focus:ring-emerald-100"/><div className="mt-3 flex items-center justify-between"><span className="text-xs text-slate-400">{value.length}/500 characters</span><button disabled={loading||!value.trim()} onClick={onGenerate} className="flex items-center gap-2 rounded-xl bg-forest px-5 py-2.5 font-semibold text-white transition hover:bg-emerald-800 disabled:cursor-not-allowed disabled:opacity-50">{loading?<LoadingSpinner/>:<Sparkles size={17}/>} Generate SQL</button></div></section>}
