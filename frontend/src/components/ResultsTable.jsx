import {Download} from 'lucide-react';
export default function ResultsTable({results,rowCount,message,onExport}){if(results===null)return <section className="card grid min-h-48 place-items-center p-8 text-center text-slate-400">Results will appear here after you run a query.</section>;let columns=results[0]?Object.keys(results[0]):[];return <section className="card overflow-hidden"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-semibold">Results</h2><p className="text-xs text-slate-500">{rowCount?`Showing ${rowCount} result${rowCount===1?'':'s'}`:message}</p></div>{rowCount>0&&<button onClick={onExport} className="flex items-center gap-2 rounded-lg border px-3 py-2 text-sm hover:bg-slate-50"><Download size={15}/> CSV</button>}</div>{results.length?<div className="max-h-96 overflow-auto"><table className="w-full border-collapse text-left text-sm"><thead className="sticky top-0 bg-slate-50"><tr>{columns.map(c=><th key={c} className="whitespace-nowrap border-b px-4 py-3 font-semibold">{c}</th>)}</tr></thead><tbody>{results.map((r,i)=><tr key={i} className="border-b border-slate-100 hover:bg-emerald-50/40">{columns.map(c=><td key={c} className="whitespace-nowrap px-4 py-3 text-slate-600">{r[c]??'—'}</td>)}</tr>)}</tbody></table></div>:<div className="grid h-36 place-items-center text-sm text-slate-400">{message}</div>}</section>}
